	/** Gets %NAME% Property as %PROPERTY_TYPE_OVERLOAD%

 	<example>
	You want to ask for the current property value
	<code>
	%COMPONENT_NAME%.get%PROPERTY_CAPITAL_CUT%();<br>
	</code>
	</example>
	 */
	%PROPERTY_TYPE_OVERLOAD% get%PROPERTY_CAPITAL_CUT% () {*		
		var elementsJson = JSON.parse(this.%PROPERTY_CAPITAL%);
		return elementsJson;
	*}